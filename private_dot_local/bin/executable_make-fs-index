#!/bin/sh

index_file="$HOME/.local/fs-index"

echo -n "" > "${index_file}"

dirs_to_index="
    $HOME/cirno/nas/Documents/Docs
    $HOME/cirno/nas/Books
    $HOME/cirno/nas/Exers
    $HOME/cirno/nas/Pictures
"

for dir in ${dirs_to_index}; do
    rg --files "${dir}" >> "${index_file}"
done
